const Product = require('../models/product')
const asyncHandel = require('express-async-handel')

const createProduct = asyncHandel(async(req, res) => {
    y
})
module.exports = {
    createProduct
}